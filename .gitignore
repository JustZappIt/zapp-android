syntax: glob

# Build and intermediate files
build/
bin/
gen/
.gradle/
.kotlin/
.DS_Store
local.properties
*.iml
*.hprof
*.apk
*.aab
*.keystore

# IDE-specific files
.idea/AndroidProjectSystem.xml
.idea/androidTestResultsUserPreferences.xml
.idea/appInsightsSettings.xml
.idea/artifacts
.idea/assetWizardSettings.xml
.idea/caches
.idea/compiler.xml
.idea/csv-editor.xml
.idea/deploymentTargetDropDown.xml
.idea/deploymentTargetSelector.xml
.idea/dictionaries/
.idea/gradle.xml
.idea/inspectionProfiles/Project_Default.xml
.idea/kotlinc.xml
.idea/libraries/
.idea/material_theme_project_new.xml
.idea/migrations.xml
.idea/modules.xml
.idea/other.xml
.idea/project.prompts.xml
.idea/runConfigurations.xml
.idea/studiobot.xml
.idea/tasks.xml
.idea/vcs.xml
.idea/workspace.xml
.idea/deviceManager.xml
.idea/markdown.xml
.idea/ChatHistory_schema_v3.xml

# Other configuration files
google-services.json
.settings

# Credentials and key material
*.jks
*.p12
*.pem
.env
.env.*
id_rsa
id_ed25519

# Editor and agent scratch state
.vscode/
.git-side-work/

# Dependencies
**/node_modules/

# ---------------------------------------------------------------------------
# Publication boundary
#
# This repository is the published mirror of a private development repository.
# Everything below exists there and is deliberately not published here. The
# rules are what enforce that: without them a routine `git add -A` after a sync
# silently republishes material that was removed on purpose. If you are adding
# something to the private tree that must not ship here, add it here too.
# ---------------------------------------------------------------------------

# Continuous integration runs here. It builds against the published messaging
# SDK rather than the private sibling it is released from, so the pull request
# pipeline and the composite actions it calls are part of this repository and
# stay green on contributions from forks, which never receive secrets. The
# optional device-test jobs still read secrets and skip when they are absent.
#
# Deployment and release remain unpublished: they carry signing material and
# store account state that has no counterpart here.
.github/scripts/
.github/dependabot.yml
.github/java-upgrade/
.github/modernize/

# Release operations reference account state; the audits and the parity ledger
# record unfixed findings, embargoed upstream security work, unreleased branch
# names and internal ticket numbers.
docs/PLAY_PUBLISHING.md
docs/RELEASE_SIGNING.md
docs/audits/
docs/notifications/PUSH_NOTIFICATION_AUDIT.md
docs/FORK_MAINTENANCE_GUIDE.md
docs/UPSTREAM_SECURITY_SYNC_GUIDE.md
docs/CI.md
BLIND_PEER_DEBUG.md
docs/android/
docs/play/
docs/GooglePlayStoreListing/

# In-flight planning documents. These describe work before it is decided, and
# routinely contain conclusions that were later reversed.
docs/*_PLAN.md
docs/HACKATHON.md
docs/HANDOVER_UI_CLEANUP.md

# Agent and assistant configuration, and local scratch clones.
.claude/
.claude-flow/
CLAUDE.md

# Solves a captcha to drip testnet funds. Not something to hand out.
scripts/testnet-faucet.mjs

# Design prototypes: raw capture dumps, not a build input.
# Anchored to the repo root on purpose: unanchored, `design/` also matches the real source package
# ui-design-lib/src/main/java/co/electriccoin/zcash/ui/design/, so every *new* design-system file
# was being silently ignored. The existing ones survived only because they predate the rule.
/design/

# Store artwork inherited from upstream, removed rather than republished as
# this project's own. See stores/solana/media/README.md.
stores/solana/media/*.png
fastlane/metadata/android/*/images/

# Solana dApp Store config (stored privately in GCS)
stores/config.yaml

# Installed dependencies. The blind-push deployment directory is meant to have
# `npm ci` run inside it, so without this a routine `git add -A` there commits
# the whole dependency tree.
**/node_modules/
